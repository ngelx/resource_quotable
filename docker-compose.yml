version: '3.9'
services:
  web:
    build: .
    # command: ./start_docker_development.sh
    volumes:
      - .:/myapp
    environment:
      BUNDLE_APP_CONFIG: /myapp/.bundle
    ports:
      - "3000:3000"
